"use client"; import { useState } from "react"; import { Task } from "@/types/task"; import { toast } from "@/utils/toast"; interface Props { onAdd: (task: Task) => void; } export const NewTaskForm = ({ onAdd }: Props) => { const [title, setTitle] = useState(""); const [submitting, setSubmitting] = useState(false); const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); if (!title.trim()) return; setSubmitting(true); try { const newTask: Task = { id: Date.now().toString(), title: title.trim(), status: "pending", completed_at: null, created_at: new Date().toISOString(), updated_at: new Date().toISOString() }; onAdd(newTask); setTitle(""); setSubmitting(false); toast.success("Tarefa criada"); } catch (error) { console.error(error); toast.error("Erro ao criar a tarefa"); } finally { setSubmitting(false); } }; return ( <form onSubmit={handleSubmit} className="flex flex-col sm:flex-row gap-3 mb-6"> <input type="text" placeholder="Nome da nova tarefa..." value={title} onChange={(e) => setTitle(e.target.value)} className="flex-1 px-4 py-2 border border-gray-300 rounded-md" required /> <button type="submit" disabled={submitting} className="px-6 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-700" > {submitting ? "Adicionando…" : "Adicionar"} </button> </form> ); };