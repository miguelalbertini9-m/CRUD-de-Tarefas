"use client"; import { useState } from "react"; import { Task } from "@/types/task"; import { toast } from "@/utils/toast"; interface Props { task: Task; onUpdate: (updates: Partial<Task>) => void; onDelete: () => void; } const statusOptions = [ { value: "pending", label: "Pendente" }, { value: "in-progress", label: "Em andamento" }, { value: "completed", label: "Concluída" }, ] as const; export const TaskCard = ({ task, onUpdate, onDelete }: Props) => { const [editing, setEditing] = useState(false); const [title, setTitle] = useState(task.title); const [status, setStatus] = useState(task.status); const [saving, setSaving] = useState(false); const handleSave = () => { if (!title.trim()) return; setSaving(true); const updates: Partial<Task> = { title: title.trim(), status, completed_at: status === "completed" ? new Date().toISOString() : null, updated_at: new Date().toISOString() }; onUpdate(updates); setSaving(false); toast.success("Tarefa atualizada"); }; const handleDelete = () => { onDelete(); toast.success("Tarefa excluída"); }; if (editing) { return ( <div className="bg-white rounded-xl p-4 space-y-3"> <div className="flex flex-col sm:flex-row gap-2"> <input type="text" value={title} onChange={(e) => setTitle(e.target.value)} className="flex-1 px-3 py-2 border border-gray-300 rounded-md" autoFocus /> <button onClick={handleSave} disabled={saving} className="px-4 py-2 bg-green-600 text-white rounded-md hover:bg-green-700" > {saving ? "Salvar…" : "Salvar"} </button> </div> <div className="flex items-center gap-2"> <select value={status} onChange={(e) => setStatus(e.target.value as Task["status"])} className="border border-gray-300 rounded-md p-1" > {statusOptions.map((opt) => ( <option key={opt.value} value={opt.value}> {opt.label} </option> ))} </select> <button onClick={handleDelete} className="text-sm text-red-600 hover:underline" > Excluir </button> </div> </div> ); } return ( <div className="bg-white rounded-xl shadow p-4 flex justify-between items-center"> <div className="flex items-center gap-2 flex-1"> <span className={`font-medium text-lg ${ status === "completed" ? "line-through text-gray-400" : "text-gray-800" }`} > {title} </span> <div className="status-toggle"> <span className={`w-4 h-4 rounded-full border-2 border-gray-500 ${ status === "completed" ? "bg-green-500" : "bg-gray-200" }`} onClick={() => { setStatus(status === "completed" ? "pending" : "completed"); }} /> <span className="ml-2 text-sm text-gray-500"> {status === "completed" ? "Concluída" : "Pendente"} </span> </div> </div> <div className="action-buttons flex gap-2"> <button onClick={() => setEditing(true)} className="text-sm text-blue-600 hover:underline" > Editar </button> <button onClick={handleDelete} className="text-sm text-red-600 hover:underline" > Excluir </button> </div> </div> );